# AWS RDS PostgreSQL connection
# Create a database on your existing instance, e.g. rave_reviews
DATABASE_URL=postgresql://USERNAME:PASSWORD@your-instance.region.rds.amazonaws.com:5432/rave_reviews?sslmode=require

# Set to false only for local Postgres without SSL
# DATABASE_SSL=false
